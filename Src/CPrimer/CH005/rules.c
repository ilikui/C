#include <stdio.h>
/**
 * @brief  优先级测试
 * 
 * @param argc 
 * @param argv 
 * @return int 
 */
int main(int argc, char const *argv[])
{
    int top, score;
    top = score =-(2+5)*6+ (4+3 * (2+3));
    printf("top = %d, score = %d\n", top, score);

    return 0;
}
