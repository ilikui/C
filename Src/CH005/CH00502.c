#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

#if 0
static int addNum(int a,int b);
int main(int argc, char *argv[]) {
	
	int a =12;
	int b =13;
	

	
	printf("sum = %d",addNum(a,b));
	
	return 0;
}


static int addNum(int a,int b)
{
	int c =12;
	
	return c;
}
#endif
